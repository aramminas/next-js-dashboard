import { Inter, Lusitana, Preahvihear } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({ weight: '700', subsets: ['latin'] });
export const preahvihear = Preahvihear({ weight: '400', subsets: ['latin'] });
