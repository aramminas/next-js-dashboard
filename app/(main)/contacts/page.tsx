import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contacts',
};

export default function Contact() {
  return (
    <section className="container mx-auto mt-[35px] flex-1">Contact</section>
  );
}
